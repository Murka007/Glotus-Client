export const enum EDanger {
    NONE,
    LOW,
    MEDIUM,
    HIGH,
}

export const enum ESentAngle {
    NONE,
    LOW,
    MEDIUM,
    HIGH,
}

export const enum EResourceType {
    WOOD,
    FOOD,
    STONE,
    GOLD,
}